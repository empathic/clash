meta:
  name: error — double uninstall
  description: Uninstalling when not installed is handled gracefully

steps:
  - name: uninstall when never installed
    command: "uninstall --yes --agent claude"
    expect:
      not:
        stderr_regex: "panic|RUST_BACKTRACE"
