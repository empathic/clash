# Policy generated by `clash migrate` from Claude Code settings.
# Review and customize as needed.
#
# Evaluation: all matching statements are collected, then precedence applies:
#   deny > ask > allow
# If no statement matches, the default effect is used.

[policy]
default = "ask"

[[statements]]
effect = "allow"
entity = "agent"
verb = "execute"
noun = "cargo build *"

[[statements]]
effect = "allow"
entity = "agent"
verb = "execute"
noun = "cargo clippy *"

[[statements]]
effect = "allow"
entity = "agent"
verb = "execute"
noun = "find *"

[[statements]]
effect = "allow"
entity = "agent"
verb = "execute"
noun = "cargo test *"
